import { ConfirmationClient } from "./ConfirmationClient";

export const metadata = {
  title: "Launchbelt — Supplier application submitted",
};

export default function SupplierConfirmationPage() {
  return <ConfirmationClient />;
}
